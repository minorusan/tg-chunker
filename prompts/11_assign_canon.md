<!--
CANON ASSIGNMENT — re-tag a chunk against the FINAL canonical taxonomy (the labels the trained
extractor speaks), so corpus metadata and extractor vocabulary are one language.
Slots: {{TEXT}} — the chunk; {{TAXONOMY}} — numbered canonical domains.
-->
Knowledge chunk from a dental clinic KB:
"{{TEXT}}"

Domain taxonomy:
{{TAXONOMY}}

Which 1-3 domains does this chunk belong to? JSON array of numbers only.
